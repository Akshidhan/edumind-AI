# Attention Score Prediction – Dependencies
# Use tensorflow-cpu on servers (no GPU, much smaller install, uses less RAM)
tensorflow-cpu>=2.13,<2.16
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0
requests>=2.31
mediapipe>=0.10
opencv-python-headless>=4.8
pillow>=10.0
# streamlit is only needed locally for the demo UI – omit on server to save memory
# streamlit>=1.29